#include "Globals.h"
#include "Game.h"
#include "Wonder.h"
#include "RawMaterial.h"

int main() {

    /*
	vector<Wonder> wonders {
	    Wonder("The Appian Way", Cost(RessourceMap{{Stone, 2},{Wood, 1}}), vector<Impact>()),
	    Wonder("Circus Maximus", Cost(RessourceMap{{Glass, 1},{Clay, 1},{Wood, 1}}), vector<Impact>()),
	    Wonder("The Collossus" , Cost(RessourceMap{{Clay, 2},{Wood, 2}}), vector<Impact>())
    };
	cout << "The number of wonders is:" << wonders.size() << endl;

    vector<RawMaterial> buildings {
        RawMaterial(Wood), RawMaterial(Wood), RawMaterial(Wood), RawMaterial(Glass), RawMaterial(Glass, 2),
        RawMaterial(Clay, 2), RawMaterial(Stone, 1)
    };

	Player A("Gabriel"), B("Catherine");

    */

	cout << "Welcome to 7 Wonders Duel C++ version" << endl;
	cout << "The players are: " << endl;
	// A.print();
	// B.print();

	// Game game(&A, &B);

    return 0;
}
